<div class="follow-instagram-home">
  <div class="mainslider-follow-instagram-home"> 
       <div class="text-center  ">
     <h3 class="title ">{{ section.settings.title }} </h3>
   </div>

       <div class="love-our-product-slider" data-flickity='{ "freeScroll": true, "wrapAround": true, "groupCells": true, "prevNextButtons": true,    "autoPlay": 5000 }'>
      {% for block in section.blocks %}
      <div class="follow-instagram-home-cell">
        <a href="{{ block.settings.link }}">
          <img src="{{ block.settings.image | img_url: 'master' }}" alt="">
        </a>
        <div class="lvoe-upper">
          <h3>{{ block.settings.NameUser }}</h3>
          <h6>{{ block.settings.instagramlink }}</h6> 
        </div>
      </div>
      {% endfor %}
    </div> 
 
  </div>
</div>

{% schema %} 
   {
  "name": "Follow Instagram Home",
  "tag": "section",
  "class": "follow-instagram-home",
   "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Heading",
        "default": " Home Follow Instagram "
      }],
  "blocks": [
    {
      "type": "column",
      "name": "Follow Instagram Home",  
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        } , 

          {
          "type": "text",
          "id": "NameUser",
          "label": "Name"
        } ,
        {
        "type": "text",
        "id": "instagramlink",
        "label": "Link"
      } 
        
      ]
    }
  ],

  "presets": [
    {
      "name": "Follow Instagram Home"
    }
  ]
}

{% endschema %}

 